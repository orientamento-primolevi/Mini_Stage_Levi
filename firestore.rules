rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Come nella console: i browser non leggono né scrivono dati personali.
    // Solo il backend con Admin SDK e le autorizzazioni IAM gestisce i dati.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
